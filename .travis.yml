language: python
python:
  - "2.7"
matrix:
  include:
    # Test on pypy without coverage, because it's unnecessary and very slow.
    - python: "2.7"
install:
  - "pip install -r requirements.txt"
sudo: false
script:
  - "`which trial` vxturn"
deploy:
  provider: pypi
  user: Praekelt
  password:
    secure: "TODO"
  on:
    tags: true
    all_branches: true
